name: secret-scan

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1" # weekly full-history sweep, Mondays 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    uses: FlowMatrix-AI/.github/.github/workflows/gitleaks.yml@90457921c096771c5ffd05a5fa3f9f51db72a25e # main
